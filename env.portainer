# =================================================================
# CONFIGURAÇÕES PARA DEPLOY NO PORTAINER
# =================================================================
# Copie estas variáveis para o Portainer na seção Environment Variables

# Configurações do Supabase
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=sua-chave-publica-supabase
SUPABASE_DB_PASSWORD=senha-super-secreta-postgres-123

# Configurações do Google Gemini AI
VITE_GOOGLE_GEMINI_API_KEY=sua-chave-gemini-ai

# Configurações da Evolution API (WhatsApp)
VITE_EVOLUTION_API_URL=https://api.seudominio.com.br
VITE_EVOLUTION_API_KEY=sua-chave-evolution-api
VITE_EVOLUTION_INSTANCE_NAME=elevroi

# Configurações de WebSocket
VITE_WEBSOCKET_URL=wss://seudominio.com.br:3001

# Configurações do Redis
REDIS_PASSWORD=senha-redis-123

# Configurações do Grafana
GRAFANA_PASSWORD=admin-grafana-123
GRAFANA_USER=admin

# Configurações de Domínio
DOMAIN=seudominio.com.br

# Configurações de SSL (opcional)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# =================================================================
# INSTRUÇÕES:
# 1. Substitua TODOS os valores acima pelos seus valores reais
# 2. No Portainer, vá em Stacks > Add Stack
# 3. Cole o conteúdo do arquivo portainer-stack-production.yml
# 4. Na seção Environment Variables, adicione todas as variáveis acima
# 5. Deploy da stack
# ================================================================= 